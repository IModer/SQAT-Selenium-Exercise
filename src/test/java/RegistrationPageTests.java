import org.junit.Test;
import static org.junit.Assert.assertTrue;

public class RegistrationPageTests extends BaseTest {

    /* @Test
    public void testFillRegistrationForm() {
        driver.get("http://liftzone.hu/?o=registerform");
        RegistrationPage registrationPage = new RegistrationPage(driver);

        registrationPage.getRegisterUsernameField().sendKeys("testuser123");
        registrationPage.getRegisterEmailField().sendKeys("testuser123@example.com");
        registrationPage.getRegisterPasswordField().sendKeys("P@ssword1");
        registrationPage.getPasswordRetypeField().sendKeys("P@ssword1");
        registrationPage.getRegisterDateField().sendKeys("1990-01-01");

        if (!registrationPage.getElfogadasCheckbox().isSelected()) {
            registrationPage.getElfogadasCheckbox().click();
        }

        assertTrue(registrationPage.getCaptchaField().isDisplayed());

        registrationPage.getRegisterKuldesGomb().click();

        // TODO: Add assertion for registration success or confirmation message
    } */
}
